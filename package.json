{
  "name": "wb-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "API-first website builder: build responsive sites from CLI, REST API, or MCP (AI agents)",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "e2e": "pnpm --filter @wb/e2e e2e"
  }
}
