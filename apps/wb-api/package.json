{
  "name": "@wb/wb-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Serverless (Vercel) entry for the wb REST API + editor, backed by Turso + R2",
  "scripts": {
    "build": "tsc -p ."
  },
  "dependencies": {
    "@wb/core": "workspace:*",
    "@wb/editor": "workspace:*",
    "@wb/server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "@vercel/node": "^5.1.8",
    "fastify": "^5.10.0",
    "typescript": "^5.7.3"
  }
}
